spring:
  application:
    name: "telegabotat"

server:
  port: 8484

  datasource:
    url: jdbc:postgresql://93.189.230.154:5432/telegabotbd
    username: telegabotbd
    password: telegabotbdadil
    driverClassName: org.postgresql.Driver

  jpa:
    database: postgresql
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQL10Dialect
    hibernate:
      ddl-auto: validate
    properties:
      hibernate.jdbc.time_zone: UTC
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect